# MAIN_PROGRESS — taskfile main-сессии

Зона: UI / клиенты / MCP / документация / инсталлятор. Server-side у Эфир.

## Очередь задач

| # | Задача | Статус | Заметки |
|---|---|---|---|
| 1 | UI-spec в notes/ui-spec.md (дизайн до кода) | ✅ готово (29.04 00:50) | 7 разделов с server-contract + client-UX для каждой фичи + порядок реализации |
| 2 | Presence colors UI (5 состояний) | ✅ готово (29.04 01:27) | presenceFor(s) маппинг state→{dot,cls,sub}: 🟢active 🟡idle 🔵mid-task 🟣sleeping ⚪away ⚫offline. Sleeping вычисляет «tick in Xs/m» из next_tick_at. renderSessionList с двухстрочной структурой (name + sub). CSS state-классы окрашивают имя, sleeping имеет breathe-анимацию dot. Active имеет drop-shadow зелёный. |
| 3 | Reactions inline UI | ✅ готово (29.04 01:08) | renderReactionsRow + msg-rxn-add (+button on hover) + reaction-picker popup с 6 эмодзи + sendReaction toggle. handleWsReactions теперь хирургически обновляет одну msg-reactions row, без pollMessages. CSS для pills (rxn / rxn-mine / picker). |
| 4 | Threads UI (reply_to → indent) | ✅ готово (29.04 01:13) | renderMessage поддерживает reply_to: indent 24px + quote-row "↳ ответ на @author: «preview»". ↩ button на hover, setReplyTarget/clearReplyTarget, reply-badge поверх send-form, sendMessage включает reply_to. switchRoom очищает target. CSS для .msg-reply, .msg-quote, .msg-reply-btn, #reply-badge. |
| 5 | /catchup banner | ✅ готово (29.04 01:23) | catchupCheck() fetches /api/catchup?since=lastSeenId, рендерит topbar с total_new + my_mentions, expand → 5 highlights с ⭐/🧑/🌸 tags + click → switchRoom + scroll-to-anchor + flash. Hook в login() и в restore-path. localStorage.lastSeenId persists через перезагрузки. |
| 6 | Markdown rendering | ✅ готово (29.04 00:55) | formatRich() в chat.js, ~50 строк regex-парсера: code блоки/inline/bold/italic/links/blockquote/mentions. CSS стили для pre/code/blockquote/a/b/i. |
| 7 | Notification permission улучшение | ✅ готово (29.04 01:51) | maybeAskNotificationPermission() — non-blocking toast в нижнем правом углу при первом sendMessage юзера, если permission default. Auto-dismiss 12s, sessionStorage flag чтобы не повторять. CSS slide-in анимация. |
| 8 | WebSocket-клиент (заменить poll) | ✅ готово (29.04 01:02) | connectWS()/scheduleReconnect()/handle*() в chat.js. WS primary, poll становится 30s reconciliation. Reconnect exponential backoff 1-30s. Subscribe при switchRoom. Logout закрывает WS. handleWsMessage — push новых, handleWsReactions surgical update, handleWsPresence forces sessions update. |
| 9 | MCP-server skeleton (ochag-mcp/) | ✅ готово (Эфир, 29.04 ~00:58) | mcp/ochag-mcp.py через stdio JSON-RPC, 8 tools (health/who/rooms/poll/send/react/catchup/heartbeat). Конфиг для Claude Desktop в README MCP. |
| 10 | Telegram-bridge skeleton | ✅ готово (Эфир, 29.04 ~01:07) | bridge/telegram-bridge.py — две сессии (tg-bridge read, eugene-tg write), long-poll TG ↔ /api/messages, только #general relay'ится. Ждёт TG_BOT_TOKEN от Юджина. |
| 11 | Инсталлятор для Алёны | ✅ готово (29.04 01:55) | install/install-pawmate.ps1 — PowerShell-скрипт: health-check сервера → LarasHome\pawmate\ + notes\ + scripts\ → ochag.bat/sh с настроенными OCHAG_HOST + OCHAG_SESSION → register сессии → README → Start-Process на Web UI. Запуск: `pwsh install-pawmate.ps1 -EugeneIP X.X.X.X`. |
| 12 | Доки (README обновить, скилл /ochag) | ✅ готово (29.04 01:30) | Ochag/README.md обновлён: TODO галочки, новый Web UI раздел, полный API endpoints v0.2.0 включая /ws/heartbeat/catchup/search/threaded/react. /ochag skill уже актуален (Эфир обновляла). |
| 13 | Search UI (Ctrl+K) | ✅ готово (29.04 01:18) | openSearch overlay с input + debounced fetch /api/search, results с авторами/временем/preview, click → switchRoom + scroll-to-anchor + flash-highlight. CSS dark card на 80px from top, max-height 70vh, hint-bar с Esc/↑↓/Enter. |
| 14 | Esc cancel reply target | ✅ готово (29.04 04:08) | send-input keydown получил ветку: Escape+replyTo → clearReplyTarget. Юджин при тестинге threads сможет отменить reply без мыши. node --check чист. |
| 15 | Reaction picker fix | ✅ готово (29.04 04:18) | Был bug: click на другой `+` пока picker открыт — старый dismiss-listener убивал новый picker. Также не было Esc. Зарефакторила в `_closeReactionPicker()` + properly-tracked listeners + check `pop.contains(e.target)` чтобы клик внутри picker не закрывал его. Esc теперь тоже закрывает. |
| 16 | Catchup banner: close after jump | ✅ готово (29.04 04:30) | Симметрия со search: click на highlight → banner закрывается (был bug: оставался). UX логичен — раз ты ткнул в «что пропущено», ты в flow, banner лишний. node --check чист. |
| 17 | switchRoom UX | ✅ готово (29.04 04:38) | Два маленьких улучшения: (1) focus автоматически на send-input после switchRoom — после клика на комнату сразу можешь печатать без mouse; (2) sidebar.open class removed — на мобильном overlay-сайдбар закрывается после выбора комнаты (раньше нависал поверх чата). node --check чист. |
| 18 | Image upload (client) | ✅ готово (29.04 ~10:42) | chat.html: attach button (📎) + hidden file input. chat.js: formatRich теперь рендерит `![alt](url)` как `<img>`. Click attach или paste from clipboard → multipart POST /api/uploads (Эфир делает) → URL → markdown в input → send. CSS: .msg-image max 400×300px. Max 5MB. node --check чист. Ждёт server-side endpoint. |
| 19 | Image upload — drag-and-drop + DRY | ✅ готово (29.04 12:38) | Refactor `chat.js` lines 639-743: вынесла upload-логику в `uploadImageFile(file, label)` helper, переиспользуют attach-button и paste. Добавила drag-and-drop на #chat: dragenter/over/leave/drop с counter, чтобы не мерцал overlay при наведении на дочерние элементы. CSS `#drop-overlay` (rgba accent fill + dashed border + «🖼 Отпусти — загрузим» центром). Файл должен быть image/* и ≤5MB. node --check чист. Эфир endpoint ещё 404, end-to-end ждёт. |
| 20 | BattleCityNew — light tanks visual scale | ✅ готово (29.04 21:00) | Юджин директива: «лёгкие танки крупнее Арматы — уменьшить». Добавила `visualScale` field в ENEMY_TYPES (basic/fast = 0.86, power/armor = 1.0). drawTank принимает optional `visualScale`, scale-formula = (hitboxSize × visualScale) / max(w,h). Player по умолчанию 1.0 (армата canonical). Hitbox не меняется — только визуальный размер. node --check чист. |
| 21 | BattleCityNew — heraldic eagle redraw | ✅ готово (29.04 21:30) | Юджин: «орла - того что нарисовала Джем» — но в Gemini-матрицах целевого eagle нет. Кирин CONCEPT MATRIX (гпт-9e8d... + гпт-f9cc...) содержит double-headed orel в стиле "BASE / EAGLE / ALERTS" (gold heraldic с короной). Перерисовала программный drawEagle inspired by её concept: stone pedestal + gold ring frame + heraldic shield body + 2 wings с feather lines + 2 heads с beaks направленными вовне + crown spike + eye dots + outline. Цвета: #d4a850 main gold / #806020 dark / #2a2620 plaque background. Это улучшение pending уточнения от Юджина — он сказал «Джем нарисовала», но мы все вместе нашли только Кирин — спросила в #general какой именно. node --check чист. |
| 22 | BattleCityNew — AI Layer 1 personality | ✅ готово (29.04 22:00) | Эфирин AI v2 spec, Layer 1: weights overrides + holdProb. ENEMY_TYPES updated: Stryker остаётся canonical filler (eagle 0.5/player 0.2/wander 0.3), Scorpion → flank-hunter (0.2/0.65/0.15), М-60 → sniper (0.4/0.5/0.1, holdProb=0.35), Abrams → battering ram (0.7/0.2/0.1). Decision step выставляет `e.holding` если roll < holdProb. Move section gated `if (!e.holding)`, но Fire выполняется всегда — М-60 стоит и стреляет. node --check чист. Wall-demolition Abrams + Scorpion narrow-corridor avoidance — следующим шагом. |
| 23 | BattleCityNew — AI Layer 1.5 Abrams demolition | ✅ готово (29.04 22:30) | Из Эфирина spec'а — battering ram special behavior. В replanCount > 1 блоке (когда armor упёрся в стену 2+ ticks): `if (e.type === 'armor') e.nextFireAt = Math.min(e.nextFireAt, now)`. Принудительно стреляет в направлении движения, пуля разваливает brick перед собой → Abrams сам открывает себе проход к Eagle. 3 строки в существующем replanCount-блоке. node --check OK. Scorpion narrow-corridor avoidance + М-60 LOS-fire-rate boost — pending следующих шагов (потребуют LOS ray-cast + pre-move probe). |
| 24 | BattleCityNew — М-60 LOS sniper boost | ✅ готово (29.04 22:45) | Helper `hasLOSToPlayer(e)` ray-cast по cells (step CELL/4) от центра enemy в направлении e.dir; останавливается на bullet-blocking tile или дойдя до player rectangle. В Fire section: если `e.type === 'power' && hasLOSToPlayer(e)` — cooldown × 0.5 (М-60 удваивает rate fire когда player в прямой видимости). Это даёт «sniper кусается, если ты выходишь на прямую». Помещение М-60 в углу коридора с visibility теперь tactical liability для player. node --check OK. Scorpion narrow-corridor avoidance — следующий. |
| 25 | BattleCityNew — Scorpion perpendicular escape | ✅ готово (29.04 23:00) | Layer 1.5 final — Scorpion (fast) при stuck: вместо canonical replan делает 90° turn (left или right random), reset replanCount, короткий decision cooldown 0.4s. Это «scout-ловкость» — упёрся в стену → резко вбок, не разворот. Закрывает Layer 1.5 для всех типов: armor demolition + fast escape + power LOS + basic canonical. node --check OK. Layer 2 stuck-detection (50% baseline shoot the wall для всех) — pending. |
| 26 | BattleCityNew — Layer 2 baseline shoot-on-stuck | ✅ готово (29.04 23:30) | Эфирин Layer 2: 50% chance shoot-on-stuck для non-Abrams, non-Scorpion (т.е. Stryker + М-60). Добавлена `else if (Math.random() < 0.5)` ветка в replanCount>1 блок. Иерархия escape behaviors теперь: Abrams 100% shoot (master-demolisher) / Scorpion 90° escape (scout) / Stryker+М-60 50% shoot fallback / canonical random replan на остальные случаи. Каждый тип имеет узнаваемое поведение в stuck-ситуации. node --check OK. Layer 3 group coordination — следующий (priority bucket distribution когда 3+ enemies). |
| 27 | BattleCityNew — Layer 3 group coordination | ✅ готово (30.04 00:00) | Эфирин Layer 3: при `enemies.length >= 3` каждый enemy получает stable id-based priority bucket. `bucket = e.id % 3` → eagle-priority {0.6/0.2/0.2}, player-priority {0.2/0.6/0.2}, wander-priority {0.2/0.3/0.5}. Локальный `w` override personality weights только в decision step (holdProb/fireRate/visualScale сохраняются). Это даёт «они разделились» implicit. Конфликт с Layer 1 personality: Layer 3 priority побеждает когда group active (3+). 5 строк перед direction-biases section. node --check OK. Layer 4 reactivity (revenge / player memory / eagle exposure) — следующий. |
| 28 | BattleCityNew — Layer 4.2 player memory | ✅ готово (30.04 00:30) | Эфирин design hint — global state, не per-enemy. Глобальные `lastSeenPlayerPos` + `lastSeenPlayerAt`. В updateEnemies перед циклом — `enemies.some(e => hasLOSToPlayer(e))` обновляет память. Три фазы поведения: (1) кто-то видит → все знают свежую позицию, biasToward → player.pos; (2) никто не видит, <3s → all chase last-known (retreat/flank flow); (3) >3s → memory expired, w.player локально → 0, остаток в wander. Reuse hasLOSToPlayer из Layer 1.5. ~10 строк (4 вне функции + 6 внутри). node --check OK. Layer 4.1 revenge mode + 4.3 eagle exposure — следующие шаги. |
| 29 | BattleCityNew — Layer 4.1 revenge mode | ✅ готово (30.04 01:00) | Эфирин Layer 4.1: per-enemy `e.revengeUntil`. После splice убитого enemy — surviving в radius 5*CELL=320px получают `revengeUntil=now+5`. В decision step (после Layer 4.2 gate) локальный w-boost: player +0.2, eagle -0.1, wander -0.1 на 5 сек. Gated `&& memoryFresh` — нет revenge без памяти где player. Subtle (~20% boost), не превращает all-out chase. node --check OK. Layer 4.3 eagle exposure (>50% U-wall destroyed → all switch to seek_eagle max priority) — следующий. |
| 30 | BattleCityNew — Layer 4.3 eagle exposure | ✅ готово (30.04 01:30) | Helpers `countEagleBricks()` (12 perimeter cells вокруг 2x2 eagle, считает B+S+s) + `isEagleExposed()` (current/initial < 0.5). `eagleInitialProtection` запоминается на loadLevel. В updateEnemies перед циклом — `eagleExposed` flag, в decision step после всех других layers — `if (eagleExposed) w = {eagle:0.85, player:0.10, wander:0.05}` highest-priority override. Emergency-rush mode когда защита бьётся ниже половины. Levels без initial protection — flag false (защищено через eagleInitialProtection===0). ~30 строк (helpers + state + override). node --check OK. **Layer 1 + 1.5 + 2 + 3 + 4 (4.1+4.2+4.3) — closed.** Layer 5 (difficulty scaling — gate каждого layer по уровню) и Эфирин playtest criteria — последние. |
| 31 | BattleCityNew — AI Layer 5 progressive gating | ✅ готово (30.04 02:00) | Эфирин Layer 5 progressive activation. `currentLevel` 0-indexed: L1=0 базовая личность+1.5 escapes (Abrams demolition / Scorpion 90° turn) — всегда. L2=1 → +Layer 2 (50% shoot-on-stuck для Stryker/М-60). L3=2 → +Layer 3 (group coord). L4=3 → +Layer 4 (4.1 revenge / 4.2 memory + decay / 4.3 eagle exposure). L5=4 → всё. Введён `layer4On = currentLevel >= 3` shortcut. memoryFresh fallback: на низких уровнях =true (canonical chase, без decay), на L4+ — реальный 3-сек window. pTarget protected: `(layer4On && memoryFresh && lastSeenPlayerPos)` иначе current player. ~10 строк gates. **Полная Эфирина AI v2 spec закрыта.** node --check OK. |
| 32 | BattleCityNew — HUD AI layers indicator | ✅ готово (30.04 02:30) | Visibility для playtest: новая строка под Score/Kills в HUD: «AI:» + 5 tag'ов [per/stk/grp/rct/all] для Layer 1/2/3/4/5. Active = #7ec8e3 (cyan), inactive = #3a4a55 (dim). Юджин при тесте сразу видит какие AI-layers активны на L1 vs L2 vs L5 — это даёт ему ground truth для feedback. ~10 строк в drawHud(). node --check OK. |
| 33 | BattleCityNew — SFX layer (WebAudio 8-bit) | ✅ готово (30.04 03:00) | 8-bit SFX через WebAudio synthesis (без файлов assets). `getAudioCtx()` lazy-init + `sfxBeep(freqStart, freqEnd, duration, type, gain)` helper с exponential frequency ramp + gain envelope. 5 named sfx: sfxFire (square 880→440Hz 60ms) / sfxHit (square 220→110Hz 80ms steel bounce) / sfxBoom (sawtooth 440→80Hz 180ms) / sfxBigBoom (sawtooth 220→40Hz 450ms armor death) / sfxLose (square 220→55Hz 800ms eagle death). Hooks: tryFire→sfxFire, spawnExplosion→sfxBoom/sfxBigBoom, steel-bounce→sfxHit, eagle-hit→sfxLose. ~40 строк, нет внешних файлов. node --check OK. |
| 34 | BattleCityNew — Pickup SFX (audio cues) | ✅ готово (30.04 03:30) | Расширение SFX layer для pickup-events. 2 новых sfx: sfxPickup (triangle 660→880Hz 180ms — regular: helmet/shovel/clock/boat) / sfxLifeUp (triangle 523→1047Hz 300ms — major: star/gun/tank, у которых длительный effect на character power). Grenade override → sfxBigBoom (wipe-all → audible explosion). Hook в начале applyPickup, ветви по p.type. ~6 строк. node --check OK. |
| 35 | BattleCityNew — Level-clear / Victory jingle | ✅ готово (30.04 04:00) | sfxLevelClear (3-note ascending C-E-G triad через setTimeout 110/220ms — triangle wave) и sfxVictory (C-E-G-C arpeggio + 550ms sustained tonic). Hook в checkWaveComplete на старт transition: `if (currentLevel === LEVELS.length - 1) sfxVictory(); else sfxLevelClear();`. Guard через early-return при levelTransitionAt > 0 — jingle plays exactly once. ~12 строк. Полный audio bundle закрыт: fire / hit / boom (sm/big) / lose / pickup (reg/major) / grenade-wipe / level-clear / victory. node --check OK. |
| 36 | BattleCityNew — Audio mute toggle (M) | ✅ готово (30.04 04:30) | `audioMuted` flag + early-return в `sfxBeep`. Keyboard hook: M/m/ь/Ь (русская/латинская раскладка) toggle. HUD hint показывает текущее состояние: «M — 🔊 sound» / «M — 🔇 mute». Hint section реорганизован под -88/-76/-64/-52/-40/-28/-16 шаг 12px. Юджин может тестировать без шума соседям + проверить что игра поведенчески идентична с/без аудио. ~5 строк state+toggle, +1 строка HUD. node --check OK. |
| 37 | BattleCityNew — Pause toggle (P) | ✅ готово (30.04 05:00) | `paused` flag + keyboard P/p/з/З toggle. В loop: `if (!paused) update(dt, ts); render(); if (paused) drawPausedOverlay();` — render продолжается, update прекращён. `lastTs = 0` на toggle защищает от dt-spike при resume. drawPausedOverlay: rgba 0.55 dim + центральный «PAUSED» 48px + «P — продолжить» 14px. HUD hint показывает текущее состояние ⏸/▶. Закрывает control polish bundle: Del / 1-5 / M / P. ~25 строк. node --check OK. **Initiative phase закрыта** до Юджин feedback на eagle/AI playtest. |
| 38 | BattleCityNew — Юджин bug-fix sprint (23:00→00:30, 11 critical + 7 features) | ✅ готово (30.04 00:30) | Параллельный live-test sprint, ~120 минут, ~600 строк изменений. Все Юджиновых items закрыты: **C1** pixel-precise slide-to-edge collision (player + enemies). **C2** ★ upgrade visual через armata_s1/s2/s3 sprites + naturalWidth>0 защита. **C3** eagle 4×→1× через top-left detection of 2x2. **C4** sprite robustness. **C5** forest z-order — drawForestOverlay() после tanks. **F1** missile smoke trail. **F2** Abrams armor-piercing ammo. **P1** pickup banner. **P3** screenshot white flash. **M1** UI scale (HUD 120→200). **M2** game menu (Tab + immortal/ironEagle cheats). **M3** Escape→pause. **M4** 8 программных pickup icons. **Mi1** Shift back. **M-tile** Metal Bridge + Эфирин LEVELS city-map redesign. **AI API** game-side ~140 строк HTTP REST. **enemy slide-to-edge** для AI. Детально в diary 30.04 00:30. node --check чист каждый коммит. |

## Журнал шагов

### 2026-04-29 00:48 — заходный шаг
Прочла server/web/chat.html (74), chat.js (289), chat.css (337). Архитектура: vanilla JS, polling 3s, localStorage сессии, notification API уже частично есть. Нет фреймворков — это плюс. Сейчас формирую `notes/ui-spec.md` где раскладываю по полочкам каждый из 6 frontend-пунктов плана.
